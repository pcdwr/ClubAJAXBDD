var createExerciseSD = function() {

  this.When(/^I click on the create exercise button$/, function(callback) {
    // express the regexp above with the code you wish you had
    callback.pending();

  });

  this.Then(/^I can enter a new exercise$/, function (callback) {
    // express the regexp above with the code you wish you had
    callback.pending();
  });

  this.Then(/^I can save the exercise$/, function (callback) {
    // express the regexp above with the code you wish you had
    callback.pending();
  });

}

module.exports = createExerciseSD;
